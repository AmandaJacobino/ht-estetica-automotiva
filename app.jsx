/**
 * HT Estética Automotiva — App entry (legacy)
 *
 * All components now live in src/components/.
 * The real entry point is src/main.tsx → src/App.tsx.
 * This file is kept only for backwards compatibility with any
 * external script that still references app.jsx directly.
 */

export { default } from './src/App';
